/*
    Imprime un número entero con un mínimo de 5 dígitos (rellenando con espacios si es necesario).
*/

#include <stdio.h>

int main() {
    int numero = 42;
    printf("Número con mínimo 5 dígitos: %5d\n", numero);
    return 0;
}